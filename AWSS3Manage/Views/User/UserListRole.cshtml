@model List<RoleUserViewModel>
@ViewBag.Values
<div class="row pt-4">
    <div class="col-9">
        <h3 class="text-primary">@ViewBag.User</h3>
        <br />
    </div>
    @if (Model != null && Model.Count() > 0)
    {
        <table class="table table-hover" width="100%">
            <thead>
                <tr>
                    <th>Role Name</th>
                    <th>Role Description</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td width="15%">@item.RoleName</td>
                        <td width="80%">@item.RoleDescription</td>
                        <td width="5%">
                            <div class="w-100 btn-group" role="group">
                                <form method="post" asp-action="UserDeleteRole" asp-controller="User">
                                    <input type="hidden" name="RoleId" value="@item.RoleId">
                                    <input type="hidden" name="UserId" value="@item.UserId">
                                    <input type="hidden" name="RoleName" value="@item.RoleName">
                                    <input type="hidden" name="RoleDescription" value="@item.RoleDescription">
                                    <button type="submit" name="action" class="btn btn-outline-danger">Delete</button>
                                </form>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <br />
        <p><b>Not Found Result</b></p>
    }
</div>